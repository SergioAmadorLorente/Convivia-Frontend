import React from "react";

// expo-updates OTA desactivado: genera incompatibilidades de bundle
// en entornos Windows/Linux. Usar eas build para nuevas versiones.
// esta preparado para futuro EAS en cloud
const UpdateChecker: React.FC = () => null;

export default UpdateChecker;
